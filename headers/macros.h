#ifndef MACROS_H
#define MACROS_H

#define MAX(a, b) (a > b ? a : b)
#define MIN(a, b) (a < b ? a : b)

// single quotes for character constants - be careful not to use double quotes
#define XAXIS_LABEL 'X'
#define YAXIS_LABEL 'S'
#define QUIT 'Q'
#define GRAPH 'W'
#define DESC 'S'
#define usi unsigned short int

#endif
